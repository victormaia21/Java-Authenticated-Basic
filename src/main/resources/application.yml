spring:
  datasource:
    url: jdbc:mysql://localhost:3306/authentication_java
    username: victor
    password: 9345381276771
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update  # ou "create" se você quiser criar as tabelas automaticamente
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false

api:
  security:
    secret: ${JWT_SECRET:2391cdismcs921}

logging:
  level:
    org:
      springframework:
        security:
          web: DEBUG
