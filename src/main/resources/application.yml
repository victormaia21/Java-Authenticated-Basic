spring:
  datasource:
    url: jdbc:mysql://localhost:3306/authentication_java
    username: victor
    password: 9345381276771
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization:
      mode: always
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false

api:
  security:
    secret: ${JWT_SECRET:fjwefi232x83xmu8e23udsfjwifj23847823}
    
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org:
      springframework:
        security:
          web: DEBUG
